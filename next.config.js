/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "images.pexels.com", 
            "generatif.co"
        ]
    }
}

module.exports = nextConfig