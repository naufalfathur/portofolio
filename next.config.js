/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "images.pexels.com",
            "generatif.co",
            "images.ctfassets.net"
        ]
    }
}

module.exports = nextConfig